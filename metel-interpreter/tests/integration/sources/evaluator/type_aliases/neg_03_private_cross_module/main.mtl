// RFC-0160: referencing another module's non-`public` alias is a visibility
// error, the same as any other private item.
import secrets::Secret;

fun main() {
    let x: Secret := 1;
    assert(x == 1);
}
