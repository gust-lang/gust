// issue #232: .yolo() on Some/Ok returns the contained value without panicking.
fun main() {
    let some: Perhaps<i64> := Perhaps::Some { value = 7 };
    assert(some.yolo() == 7);

    let ok: Result<i64, String> := Result::Ok { value = 9 };
    assert(ok.yolo() == 9);
}
