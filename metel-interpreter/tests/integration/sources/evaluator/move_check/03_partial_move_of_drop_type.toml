[options]
move_check = true
spec = ["spec.ownership.drop.legality-1", "spec.ownership.partial-moves.legality-2"]

[expect]
status = "typecheck_error"
code = "T0019"
contains = "belongs to a `Drop` type"
