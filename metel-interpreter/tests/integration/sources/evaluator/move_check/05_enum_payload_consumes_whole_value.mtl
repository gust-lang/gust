enum MaybeText {
    Empty,
    Full { text: String },
}

fun main() {
    let value := MaybeText::Full { text = "x" };
    let n := match value {
        MaybeText::Full { text } => text.len(),
        MaybeText::Empty => 0,
    };
    let again := value;
}
