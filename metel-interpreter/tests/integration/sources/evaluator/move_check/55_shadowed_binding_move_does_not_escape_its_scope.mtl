fun main() {
    let s := "original";
    var i := 0;
    loop {
        i += 1;
        let s := "shadow";
        let moved := s;
        if (i == 2) { break; }
    }
    let outer := s;
    assert(outer == "original");
}
