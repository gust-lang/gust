struct Counter {
    value: i64,
}

fun bump(r: &var Counter) {
    r.value = r.value + 1;
}

fun main() {
    var c = Counter { value = 0 };
    let r = &var c;
    bump(r);
    bump(r);
    assert(c.value == 2);
}
