struct Handle {
    name: String,
    fd: i64,
}

extend Handle: Drop {
    fun drop(&var self) { }
}

fun main() {
    let handle = Handle { name = "x", fd = 1 };
    let n = match handle.name {
        name => name.len(),
    };
}
