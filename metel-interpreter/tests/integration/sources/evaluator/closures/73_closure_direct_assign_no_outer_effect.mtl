// Direct assignment to a captured var binding inside a closure operates on
// the closure's deep-cloned copy of the env, not the original. The outer binding
// must remain unchanged after the call.
fun main() {
    var x = 10;

    let f = () -> () {
        x = 42;
    };

    f();

    assert(x == 10);
}
