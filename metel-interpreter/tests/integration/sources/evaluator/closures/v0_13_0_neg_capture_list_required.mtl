// v0.13.0 closure cluster (RFC 0050 legality-6): a closure must carry a
// capture list if its body references a free non-Copy local binding.
//
fun main() {
    let name := "log";
    let greet := () -> String { name }; // missing capture list for `name`
    assert(greet() == "log");
}
