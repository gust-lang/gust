// v0.13.0 closure cluster (RFC 0134 legality-12): a closure value satisfies
// no aspects. `==`, `<`, `.clone()` and other aspect-gated operations on a
// closure value do not type-check.
//
fun main() {
    let a := || { 1 };
    let b := || { 1 };
    assert(a == b); // error: closures do not satisfy `Eq`
}
