public fun sum(arr: i64[]) -> i64 {         
    assert(arr.len() > 0);         
    var total = 0;         
    var i = 0;         
    while (i < arr.len()) { total += arr[i as u64]; i += 1; }         
    return total;         
}
