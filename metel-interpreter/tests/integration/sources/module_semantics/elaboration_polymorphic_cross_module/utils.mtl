// Generic utilities imported by main — tests that polymorphic calls survive elaboration.

public fun identity<T>(x: T) -> T { x }

public fun first<T>(a: T, b: T) -> T { a }

public fun apply<T>(f: (T) -> T, x: T) -> T { f(x) }
