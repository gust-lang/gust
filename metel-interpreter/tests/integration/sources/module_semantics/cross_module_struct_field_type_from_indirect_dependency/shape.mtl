import coords::Coords;
public struct Shape { public origin: Coords, public size: i64 }
public fun make_shape(x: i64, y: i64, s: i64) -> Shape {
    return Shape { origin= Coords { x= x, y= y }, size= s };
}
