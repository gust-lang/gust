// RFC-0126: `T[]` is an unconditionally Copy, non-owning view -- it never
// grants write access through its indices, regardless of let/var.
fun main() {
    let a: i64[] := [1, 2, 3];
    a[0u64] := 5;
}
