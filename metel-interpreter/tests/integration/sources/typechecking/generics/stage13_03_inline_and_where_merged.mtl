// Positive: inline bound + additional where clause bound on the same param,
// both satisfied.

aspect Printable {
    fun print(self);
}

aspect Comparable {
    fun compare(self, other: Self) -> i64;
}

struct SortedCache<T: Printable> where T: Comparable {
    items: T[],
}

struct Entry {
    value: i64,
}

extend Entry: Printable {
    fun print(self) {}
}

extend Entry: Comparable {
    fun compare(self, other: Entry) -> i64 {
        self.value - other.value
    }
}

fun make(e: Entry) -> SortedCache<Entry> {
    SortedCache { items: [e] }
}
