// Negative: `extends Aspect` is not permitted as a cast target.
aspect P { fun p(&self); }

struct L { t: String }

extend L: P { fun p(&self) {} }

fun main() {
    let value = L { t = "x" } as extends P; // ERROR[T0022]
}
