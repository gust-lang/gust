// Negative: tuple arguments do not yet have structural aspect impls.
// Expect T0012 with the Phase 1 tuple hint.

fun show<T: Display>(x: T) -> String {
    return x.to_string();
}

fun main() {
    let pair = (1, 2);
    let _ = show(pair); // ERROR[T0012]
}
