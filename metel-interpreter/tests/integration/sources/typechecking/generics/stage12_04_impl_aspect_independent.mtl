// Positive: two `extends Aspect` params in one signature are independent type variables.
// The function can be called with two different types that both satisfy the bound.

aspect Printable {
    fun print(self);
}

struct A { value: i64 }
struct B { value: String }

extend A: Printable { fun print(self) {} }
extend B: Printable { fun print(self) {} }

fun print_both(x: extends Printable, y: extends Printable) {
    x.print();
    y.print()
}

fun run(a: A, b: B) {
    print_both(a, b)
}
