// Positive: struct with inline + multi-bound; constructed with a type that
// satisfies both aspects.

aspect Printable {
    fun print(self);
}

aspect Comparable {
    fun compare(self, other: Self) -> i64;
}

struct SortedList<T: Printable + Comparable> {
    items: T[],
}

struct Item {
    value: i64,
}

extend Item: Printable {
    fun print(self) {}
}

extend Item: Comparable {
    fun compare(self, other: Item) -> i64 {
        self.value - other.value
    }
}

fun make_list(item: Item) -> SortedList<Item> {
    SortedList { items: [item] }
}
