// Positive: basic return-position extends Aspect without method calls
// Should typecheck without any method calls

aspect Display {
	fun display(&self) -> String;
}

struct MyInt {
	value: i64,
}

extend MyInt: Display {
	fun display(&self) -> String {
		self.value.to_string()
	}
}

// Basic function returning extends Display - no method calls yet
fun make_pair() -> extends Display {
	MyInt { value = 42 }
}

fun main() {
	let pair := make_pair();
	// Don't call any methods - just use the value as extends Display
	// This should typecheck without errors
}