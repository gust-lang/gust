// Negative: `impl Aspect` is not permitted inside a local let annotation.
aspect P { fun p(&self); }

struct L { t: String }

extend L: P { fun p(&self) {} }

fun main() {
    let values: impl P[] = [L { t = "x" }]; // ERROR[T0022]
}
