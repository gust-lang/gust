// Overload resolution is exact: a `boolean` argument matches neither overload,
// and no implicit coercion is attempted.
fun f(x: i32) -> i32 { x }
fun f(x: String) -> i32 { 0i32 }

fun main() {
    let _ := f(true); // ERROR[T0003]
}
