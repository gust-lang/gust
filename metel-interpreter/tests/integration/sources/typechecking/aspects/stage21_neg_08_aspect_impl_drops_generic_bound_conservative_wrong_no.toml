# Known limitation (RFC-0129 minimal interim): a safe constraint *widening* is
# rejected by the structural-equality rule. Flip to a positive fixture when
# admissible-domain-inclusion conformance lands.
[options]
error = ["T0012"]
spec = ["spec.declarations.aspects.implementing-an-aspect.legality-13"]

[expect]
status = "typecheck_error"
code = "T0012"
contains = "does not match the signature declared by aspect"
